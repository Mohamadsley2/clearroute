# Copy this file to  .streamlit/secrets.toml  and fill in your keys.
#   cp .streamlit/secrets.toml.example .streamlit/secrets.toml
#
# secrets.toml is in .gitignore — never commit it.

# Anthropic (Claude) API key — console.anthropic.com -> API Keys
ANTHROPIC_API_KEY = "sk-ant-..."

# Google Maps Platform key — console.cloud.google.com
# Required APIs: Maps JavaScript API, Directions API, Geocoding API
GOOGLE_MAPS_API_KEY = "AIza..."
